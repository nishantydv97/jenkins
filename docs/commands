1. for building docker images fromm docker-compose file and starting services 
	docker-compose up -d
	 -d is for  runnig in demon mode

2. for stoping docker-compose services
	docker-compose stop

3. for sratring docker-compose  services 
	docker-compose start

4. for restarting docker-compose  services
	docker-compose  restart <service name>

5. for  comleating deleting docker-commpose services(volume will  not be feleted from local volume)
	docker-compose down

6. to get into the container bash
	docker exec -ti <container id> bash

7. for copying a file from current dir to docker container 
	docker cp <script name> <container name>:/<path>/<script name>

8. for creating ssh-keygen
	ssh-keygen -f <filename>

9. creating mysqldump from remote_mysql container machine to local
	mysqldump -u root -h db_host -p testdb > /tmp/db.sql

10. command to uploading any file to aws s3 bucket  using aws cli is
	export AWS_ACCESS_KEY=""
	export AWS_SECRET_ACCESS_KEY=""
	aws s3 /path/data.ext s3://<bucket name>/data.ext
